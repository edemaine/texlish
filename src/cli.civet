fs from fs
{ compile } from './main.civet'

export function cli
  for filename of process.argv[1..]
    input := fs.readFileSync filename, 'utf8'
    output := compile input, {filename}
    fs.writeFileSync filename+'.tex', output
